<h1>レポート一覧</h1>

<%= link_to "新しいレポートを追加", new_task_path %>
<hr>
<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= task.title %> 
      <%= link_to "[編集]", edit_task_path(task) %>
      <%= link_to "[資料追加]", new_task_datum_path(task) %> </li>
  <% end %>
</ul>